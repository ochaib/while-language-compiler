# program with function that has same parameter name as function

# Output:
# 99

# Program:

begin
  int f(int f, int x) is
    return (f + x)
  end

  int f(int f) is
    return f
  end
  
  int x = call f(1);
  int y = call f(2);
  int z = call f(x, y);
  println x;
  println y;
  println z
end
