# program with function that has same parameter name as function

# Output:
# 99

# Program:

begin
  int f(int f) is
    return f
  end

  int f(char f) is
    return (ord f)
  end
  
  int x = call f(0);
  int y = call f('0');
  println x;
  println y
end
